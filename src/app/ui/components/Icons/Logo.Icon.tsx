
"use client";

import { FunctionComponent } from "react";

interface IScreenLoader {
    wText?: boolean
}

export const Logo: FunctionComponent<IScreenLoader> = ({ wText }) => {
    return (
        <div className="flex flex-col items-center w-max mx-auto">
            
        </div>
    );
};
